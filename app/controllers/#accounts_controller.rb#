class AccountsController < ApplicationsController

  

  private

  def account_params
    params.require(:account).permit(:user_id, :oauth_expires, :oauth_token, :provider, :uid, :username, :oauth_secret)
  end


end
